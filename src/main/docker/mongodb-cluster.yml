version: '2'
services:
    appjhipster-mongodb:
        image: mongo:3.4.10
        ports:
            - "27017:27017"
        command: mongos --configdb appjhipster-mongodb-config
    appjhipster-mongodb-node:
        build:
            context: .
            dockerfile: mongodb/MongoDB.Dockerfile
        command: mongod --replSet rs1 --noprealloc --smallfiles
    appjhipster-mongodb-config:
        image: mongo:3.4.10
        container_name: appjhipster-mongodb-config
        command: mongod --noprealloc --smallfiles --configsvr --dbpath /data/db
